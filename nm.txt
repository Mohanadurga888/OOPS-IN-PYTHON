from tkinter import *
from tkinter.ttk import *

from time import strftime

root = Tk()
root.title('CSE B A BClock')

def time():
    string = strftime('%d/%m/%y , %H:%M:%S %p')
    lbl.config(text=string)
    lbl.after(100,time)

lbl = Label(root,font=('ARIAL',40,'bold'),
            background='green',
            foreground='yellow')

lbl.pack(anchor='center')
time()
mainloop()
